class Person {
  var name = string(null);
  construct(name: string) {
    self.name = name;
  }
  def say_hello() {
    cout("Hello, ${self.name}");
  }
}

var person = Person("Ralf");
person.say_hello();
